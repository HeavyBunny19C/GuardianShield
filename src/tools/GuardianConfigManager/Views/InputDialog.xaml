<Window x:Class="GuardianConfigManager.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" Width="380" Height="170" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0" FontSize="13" Margin="0,0,0,8" TextWrapping="Wrap"/>
        <PasswordBox x:Name="InputBox" Grid.Row="1" FontSize="13" Padding="4"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="确定" Width="70" IsDefault="True" Click="Ok_Click" Margin="0,0,8,0"/>
            <Button Content="取消" Width="70" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
