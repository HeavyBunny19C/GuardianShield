Task 11: Log events to file when GuardianB is non-primary
==========================================================
Status: GREEN

Changes Made:
1. guardian_b.h — Added m_backupEventLogger (std::shared_ptr<Logger>) member to GuardianB class
2. guardian_b.cpp InitializeLogger() — Initialize m_backupEventLogger with:
   - Base path: <logDir>\backup_events (produces backup_events_YYYY-MM-DD.json)
   - Format: LogFormat::JSON (JSON Lines output)
   - Retention: same as main logger
3. guardian_b.cpp EventProcessingThread() — Replaced silent event discard with:
   - m_backupEventLogger->LogEvent() call when m_isPrimary==false
   - Logs: eventType, threatLevel ("LEVEL_0"), action ("LOG"), filePath, processName, processId
   - Detail field: "non-primary backup capture"

Behavior:
- When m_isPrimary==true: unchanged (HandleDriverEvent as before)
- When m_isPrimary==false: events written to C:\ProgramData\GuardianShield\logs\backup_events_YYYY-MM-DD.json
- File rotates daily via Logger's built-in daily rotation
- No cross-process forwarding, no IPC changes

Verification:
- Code compiles with existing Logger API (LogEvent signature matches)
- DriverEventTypeToString from common_types.h converts event.event_type to string
- No changes to primary path behavior
