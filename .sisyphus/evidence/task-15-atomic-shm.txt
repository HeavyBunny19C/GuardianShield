TASK 15: Atomic SharedMemory Timestamp Access
==============================================

OBJECTIVE:
Replace direct 64-bit timestamp assignments in SharedMemory with InterlockedExchange64 for atomic access.

CHANGES MADE:
=============

File: src/service/common/src/ipc.cpp

1. UpdateHeartbeat() - Line 565
   BEFORE: m_stateBlock->last_update = GetCurrentTimestamp();
   AFTER:  InterlockedExchange64((LONGLONG*)&m_stateBlock->last_update, (LONGLONG)GetCurrentTimestamp());

2. SetEmergencyState() - Line 585
   BEFORE: m_stateBlock->last_update = GetCurrentTimestamp();
   AFTER:  InterlockedExchange64((LONGLONG*)&m_stateBlock->last_update, (LONGLONG)GetCurrentTimestamp());

RATIONALE:
==========
- InterlockedExchange64 ensures atomic write of 64-bit timestamps in shared memory
- Prevents torn reads on 64-bit values across process boundaries
- Documents intent for atomic access explicitly
- Maintains existing mutex-based synchronization for broader state consistency
- Windows.h already includes <winnt.h> which provides InterlockedExchange64

VERIFICATION:
=============
✓ Both direct assignments to last_update field replaced with InterlockedExchange64
✓ Casts properly handle LONGLONG* pointer and value conversion
✓ No changes to SharedStateBlock layout
✓ No changes to heartbeat interval or IPC protocol
✓ Mutex-based locking still in place for broader state consistency

NOTES:
======
- The task description mentioned heartbeat_a and heartbeat_b, but the actual implementation
  uses a heartbeats[3] array. The critical 64-bit field requiring atomic access is last_update.
- GetHeartbeat() and GetEmergencyState() reads are protected by the existing mutex, so they
  don't require additional atomic operations.
- InterlockedExchange64 is available on all Windows platforms via Windows.h
