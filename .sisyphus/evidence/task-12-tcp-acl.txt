TASK: Add getpeername peer IP validation in TCP server accept loop
FILE: src/service/common/src/ipc.cpp

CHANGES MADE:
1. Modified TcpServer::AcceptLoop() (lines 691-717)
   - Added getpeername() call after accept() to validate peer address
   - Check: peerAddr.sin_addr.s_addr == htonl(INADDR_LOOPBACK)
   - Action: If NOT loopback, closesocket(clientSocket) and continue

2. Updated TLS comment (line 631)
   - Old: "TLS requested but SChannel negotiation is not yet implemented. Falling back to plaintext loopback. Phase 2.8 will add full TLS support."
   - New: "TLS deferred to V2. Plaintext loopback active with peer validation. Phase 2.8 will add full TLS support."

VALIDATION:
- TCP server already binds to INADDR_LOOPBACK (line 652: htonl(INADDR_LOOPBACK))
- New validation ensures only 127.0.0.1 connections proceed
- Non-loopback connections are immediately rejected
- Socket is closed before thread dispatch

SECURITY POSTURE:
✓ Loopback-only IPC channel enforced
✓ Peer validation active on accept
✓ Remote connections blocked at socket level
✓ No TLS implementation (deferred to V2)
