TASK 8: ProcessIntegrity::VerifyHash() Implementation
======================================================

STATUS: COMPLETE ✓

IMPLEMENTATION DETAILS:
- File: src/service/common/src/security.cpp (lines 214-227)
- Function: ProcessIntegrity::VerifyHash(const std::wstring& processPath, const uint8_t expectedHash[32])

IMPLEMENTATION LOGIC:
1. Declare actualHash[32] buffer for computed hash
2. Call Hash::SHA256File(processPath, actualHash) to compute file hash
3. Return false if SHA256File fails
4. Use constant-time XOR comparison loop (volatile uint8_t diff)
5. Compare all 32 bytes: diff |= actualHash[i] ^ expectedHash[i]
6. Return true only if diff == 0 (all bytes match)

SECURITY FEATURES:
- Constant-time comparison prevents timing side-channel attacks
- Uses volatile uint8_t to prevent compiler optimizations
- XOR-based comparison (not memcmp which is NOT constant-time)

CODE REVIEW:
✓ Function signature unchanged
✓ SHA256File() called correctly
✓ Constant-time comparison pattern matches ipc.cpp (lines 240-247)
✓ Returns false on SHA256File failure
✓ Returns true only if all 32 bytes match
✓ No new includes added
✓ Comments justified (security-critical code)

TESTING:
- Test file: test/test_security.cpp
- Tests: VerifyHashWithCorrectHash, VerifyHashWithWrongHash, VerifyHashWithNonexistentPath
- Note: Windows executable (GuardianTests.exe) built successfully in build/test/Release/
- macOS cannot execute Windows PE32+ binary, but code compiles without errors

COMMIT:
git -C /Users/default/Documents/GuardianShield add src/service/common/src/security.cpp
git -C /Users/default/Documents/GuardianShield commit -m "fix(security): implement VerifyHash for process integrity"
