Task 10 - Global Named Mutex Leader Election (Split-Brain Prevention)

Date: 2026-03-23
Repo: /Users/default/Documents/GuardianShield

Implemented changes:
1) src/service/GuardianA/src/guardian_a.cpp
   - OnStart: acquire global leader mutex at startup:
     m_leaderMutex = CreateMutexW(NULL, TRUE, L"Global\\GuardianShield-Leader");
   - OnStart: if CreateMutexW fails, log warning and continue.
   - OnStop: release and close mutex handle:
     ReleaseMutex(m_leaderMutex); CloseHandle(m_leaderMutex); m_leaderMutex = NULL;

2) src/service/GuardianA/include/guardian_a.h
   - Added private member:
     HANDLE m_leaderMutex{NULL};

3) src/service/GuardianB/src/guardian_b.cpp
   - OnStart: create mutex handle without initial ownership:
     m_leaderMutex = CreateMutexW(NULL, FALSE, L"Global\\GuardianShield-Leader");
   - OnStart: if CreateMutexW fails, log warning and continue.
   - PromoteToPrimary: non-blocking try-acquire via WaitForSingleObject(m_leaderMutex, 0)
     * WAIT_OBJECT_0 / WAIT_ABANDONED => proceed promotion
     * WAIT_TIMEOUT => do not promote
     * other failure => do not promote
   - DemoteToBackup: ReleaseMutex(m_leaderMutex)
   - OnStop: release (if primary) and CloseHandle for cleanup

4) src/service/GuardianB/include/guardian_b.h
   - Added private member:
     HANDLE m_leaderMutex{NULL};

Verification evidence:
- LSP diagnostics executed on changed files (GuardianA/B .cpp/.h).
  Result: environment-level parsing errors due macOS host without Windows SDK/service types
  (e.g., DWORD/HANDLE/SERVICE_* unresolved), not introduced by this task-specific logic.

- Build command executed:
  Command: cmake --build build
  Result: failed in this environment (cmake not installed: "command not found: cmake").

Notes:
- No heartbeat interval changes.
- No external dependencies introduced.
- No IPC protocol changes made.
