- 2026-03-23: Failover mutex tests are safest when `WaitForSingleObject(..., 0)` contention checks happen from a different thread; same-thread mutex waits are recursive and won’t model split-brain contention.
- 2026-03-23: For crash failover semantics, closing an owned mutex handle without `ReleaseMutex` allows the next waiter to receive `WAIT_ABANDONED`, matching leader-crash takeover behavior.
- 2026-03-23: Local macOS environment lacks `cmake` and does not resolve `gtest/gtest.h` for LSP, so verification evidence should capture toolchain limits explicitly when compile checks are requested.
- 2026-03-23: ETW pipeline mock tests can exercise classification through `BuildEventResponse(Config, DriverEventType)` while batch behavior remains validated via `ThreatEvaluator::CheckBatchThresholds(const DriverEvent&)` without any real ETW APIs.
- 2026-03-23: For platform-limited verification, compile the new pure-C++ translation unit (`emergency_state_machine.cpp`) directly with `clang++` and document Windows/GTest include gaps separately so task evidence still proves the extracted unit builds.
- 2026-03-23: TASK 7 COMPLETE: Hash::SHA256File() implemented with BCrypt streaming API. Opens file with CreateFileW, reads in 64KB chunks, hashes incrementally with BCryptHashData, finalizes with BCryptFinishHash. Proper error handling and resource cleanup. Commit: 79bfbeb. Evidence: .sisyphus/evidence/task-7-sha256-green.txt. Ready for Windows build/test verification.
