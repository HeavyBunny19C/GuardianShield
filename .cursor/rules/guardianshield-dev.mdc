---
description: GuardianShield 开发规范——防止已知反模式
globs: "**/*.cpp,**/*.h,**/*.yaml,**/*.bat,**/*.ps1,**/*.wxs"
alwaysApply: false
---

# GuardianShield 开发规范

## 代码重复零容忍

- GuardianA/B 的共享逻辑必须在 GuardianCore 基类中实现（重构后）
- 不允许在两个文件中维护同名函数
- 常量（白名单、路径、超时）统一在 constants.h 定义
- 结构体不允许有"B 版本"（如 ThreatAssessmentB）

## 名称映射表

| 内部代号 | 服务名 | 可执行文件 | 注册表键 |
|---------|--------|-----------|---------|
| GuardianA | WinDefenderCore | svchost_core.exe | N/A |
| GuardianB | WinDefenderHelper | svchost_helper.exe | N/A |
| GuardianC | (无服务) | winmon.exe | WindowsMonitor |

## Batch 脚本禁区

1. 行尾必须 CRLF
2. 不能有 UTF-8 BOM
3. 用 REM 不用 ::
4. 不用 EnableDelayedExpansion
5. 不在 for/f 内嵌套 powershell
6. 关键脚本无条件 pause

## 修改后验证

每次修改后运行验证：
- `rg "旧关键词"` 确认结果为 0
- 编译通过
- 相关单元测试通过
